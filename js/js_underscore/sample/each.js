// ============================================================
//  each
//
// ============================================================

_.each([50, 60, 70], function(num) { 
    log( num );
});

log();

_.each({one : 1, two : 2, three : 3}, function(num, key) { 
    log( key + ' => ' + num );
});
